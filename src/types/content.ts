export type ContentCard = {
  title: string;
  text: string;
  image?: string;
};

export type ProductPreview = {
  id: string;
  name: string;
  eyebrow: string;
  notes: string;
  body: string;
  price: string;
  image: string;
};
